on: push

name: Deploy website on push

jobs:

  web-deploy:

    name: Deploy
    runs-on: ubuntu-latest
    steps:
    name: Get latest code

    uses: actions/checkout@v3

    name: Sync files


      with:

        server: files.000webhost.com

        username: testvvvvvvvvvvvvv

        password: GRWgMPdRCxbkEHOIe%6b

        server-dir : public_html/
